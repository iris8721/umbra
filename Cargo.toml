[package]
name = "umbra"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "abort"
opt-level = 3

[profile.dev]
panic = "abort"

[build-dependencies]
cbindgen = "0.27"
